# Input

**Development workflow only.** End users: see [Quick start](../README.md#quick-start) with an explicit path to the stock `.CAP`.

Place **one** official ASUS BIOS **4301** `.CAP` here (from [Requirements](../README.md#requirements)).

```text
in/Z10PE-D8-WS-ASUS-4301.CAP   ← example name
```

```bash
npm run verify
npm run build
```

Firmware files are gitignored — do not commit them.
